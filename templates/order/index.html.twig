{% extends 'base.html.twig' %}

{% block title %}Hello OrderController!{% endblock %}

{% block body %}
	{% set total = 0 %}

	<br>
	<div class="container">
		<h2>order here</h2>
		<br>

		<table class="table table-striped">
			<thead>
				<tr>
					<td>Order Nr.</td>
					<td>Name</td>
					<td>Price</td>
					<td></td>
				</tr>
			</thead>

			<tbody>
				{% for orders in order %}
					{% set total = total + orders.price %}
					<tr>
						<td>{{orders.orderId}}</td>
						<td>{{orders.name}}</td>
						<td>{{orders.price}}</td>
						
					</tr>
				{% endfor %}
				<td></td>
				<td></td>
				<td>{{summe}}
					EUR</td>
				<td></td>
				<td></td>
			</tbody>
		</table>
	</div>
{% endblock %}
