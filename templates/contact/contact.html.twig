{% extends 'base.html.twig' %}

{% block body %}
    <br>
        <div class="container">
            <h2>Contact Us!</h2>
                <br>
                 {{  form_start(our_form) }}
                 {{  form_widget(our_form ) }}
                  <input type="submit" value="Send" class="btn btn-primary">
                 {{  form_end(our_form) }}
        </div>

{% endblock %}